<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- WatchMates App Theme - Simple base theme for Compose -->
    <style name="Theme.Watchmates" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Use basic Material theme since we're using Compose for all UI -->
        <!-- The actual Material3 theming is handled in Compose via WatchmatesTheme -->
        
        <!-- Enable edge-to-edge display -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- windowLightNavigationBar requires API 27+ so it's defined in values-v27/themes.xml -->
    </style>
    
    <!-- Splash Screen Theme -->
    <style name="Theme.Watchmates.Splash" parent="Theme.Watchmates">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>